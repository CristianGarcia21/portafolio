export default function SectionHeading({ children }) {
  return (
    <h2 className="mb-8 font-mono text-2xl font-bold uppercase tracking-tight text-emerald-400 sm:text-3xl">
      {children}
    </h2>
  );
}
